Pending:
    FIXED fix newTask function, multiple HTML elements can't have the same ID
        fix styling
    storeTaskInput(): store input fields from an individual tasks in a object
    function(): store tasks in a project
    function(): store multiple projects

    Add footer

    Pending taskEdit #1: verify if checkMain() is really needed