export function taskObj(title,description,dueDate,priority,checklist) {
    return {
        title: title,
        description: description,
        dueDate: dueDate,
        priority: priority,
        checklist: checklist
    }
}